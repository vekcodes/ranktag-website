"""
ORM models package.

No models yet — each domain (users, domains, reports, snapshots, jobs, etc.)
will get its own module and import its model class here so Alembic
autogenerate can discover them.

Convention:
    app/models/user.py
    app/models/domain.py
    app/models/report.py
    ...
    Each module re-exported below.
"""
